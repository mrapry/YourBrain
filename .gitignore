# Rust build artifacts
/target
**/*.rs.bk

# Local brain data (never commit personal memories)
*.db
*.db-wal
*.db-shm
*.ybv
/.yourbrain/

# Editor / OS
.DS_Store
Thumbs.db

# Machine-specific IDE integration (absolute binary paths; regenerate with `yb install`)
.cursor/mcp.json
.mcp.json
.trae/mcp.json
.claude/settings.json
# ...but keep the committed, portable template
!.claude/settings.example.json

# Internal docs (not published)
docs/PANDUAN.md
docs/TEKNIS.md
PLAN.md
